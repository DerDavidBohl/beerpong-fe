import { ItemListPipe } from './item-list.pipe';

describe('ItemListPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemListPipe();
    expect(pipe).toBeTruthy();
  });
});
