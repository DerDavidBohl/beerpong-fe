import { KommaSeperatedPipe } from './komma-seperated.pipe';

describe('KommaSeperatedPipe', () => {
  it('create an instance', () => {
    const pipe = new KommaSeperatedPipe();
    expect(pipe).toBeTruthy();
  });
});
